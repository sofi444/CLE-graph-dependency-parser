SENTENCE:
- id
- form
- lemma
- pos
- xpos
- morph
- head
- rel
- empty1 (?)
- empty2 (?)
(+ add_root() # add as first element of lists)
# fill sentence object


READ: 
- in filepath
# within each of en/de: dev, test, train


WRITE:
- out filepath
# add tabs and newlines, remove root


//

TO DO:
- connect to git
- gitignore
- evaluation (optimise)
- tasks from week2

! Don’t assume there are always gold heads and dependency relations
    In the blind test file, those two columns are empty

//

GIT:
- git pull # get most recent version from server
- git add [filenames] # add edited file to the index
- git commit-m "message" # commit to local repository
- git push # upload local repository to server

// Questions:
- global variables? use a class variable from within another class
- empty fields?
- self/not?